function findMinMax(array) {
	var lenSort = array.sort()
	var len = lenSort.length
	var min = 0
	var max = 0
	var minMax = []
	for (var i = 1; i < (len +1); i++)
		if (i === 1) {
			min = min + 1
			return min;
		} else if (i <1) {
			


		}
}