module.export = function () {
	aritGeo: function(array) {
		if (array === []) {
			return 0
		}
	},

	aritGeo: function(array) {
		if (array === 0 ) || (array === -1) {
			return 'Input an array'
		}
	},

	aritGeo: function(array) {
		var total = 0;
		for (var i = 2; i<array; i++){
			total = total + i;
		}
		return total;;
		return 'Arithmetic';

		}
	},

	aritGeo: function(array) {
		var total = 0;
		for (var i = 6; i<array; i++){
			total = total + i;
		}
		return total;
		return 'Arithmetic';

		}
	},

	aritGeo: function(array) {
		var total = 0;
		for (var i = 5; i<array; i--){
			total = total + i;
		}
		return total;
		return 'Arithmetic';

		}
	},

	aritGeo: function(array) {
		var total = 0;
		for (var i = 1; i < array; i++) {
			if (i % 3 === 0) {
				total = total + i;
			}
		return total;
		return 'Geometric'
		}
	},

	aritGeo: function(array) {
		var total = 0
		for (var i = 1; i<array; i++) 
			if (i % 7 === 0) {
				total = total + i;
			}
		return total;
		return 'Geometric';
		}
	},

	aritGeo: function(array) {
		for (var i = 1; i < array; i++)
			if (i + (i+1)){
				return -1
			}
		}
	}, 

	aritGeo: function(array) {
		var len = array.length;
		for (var i = 1; i<len; i++)
			if (i(i+1)/2) {
				return -1
			}

	},

	aritGeo: function(array) {
		
		for (var i = 1; i<array; i++)
			var total = 0;
			if (i +(i +1)){
				return total = total + (i^3)
			}
		return -1	
	}
}